global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: "pixi_game_server"
    static_configs:
      - targets: ["game:8110"]
    metrics_path: /metrics
