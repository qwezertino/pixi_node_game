
// Параметры сети
export const NETWORK = {
    TICK_RATE: 16, // Оптимизировано для RPG - не требует высокой точности как шутеры
    SYNC_INTERVAL: 30000, // Увеличено до 60 секунд (редкая полная синхронизация)
    BATCH_INTERVAL_MS: 50, // ~20 FPS батчинг - идеально для RPG, снижает нагрузку на сервер
};

// Параметры движения
export const MOVEMENT = {
    PLAYER_SPEED_PER_TICK: 4, // Скорость движения за тик (целые единицы)
    // Старая скорость для обратной совместимости: 100 единиц/сек при 32 тиках/сек = ~3.125 единиц/тик
};

// Параметры игрового мира (целые координаты)
export const WORLD = {
    VIRTUAL_SIZE: {
        WIDTH: 1000,  // Виртуальный мир 1000x1000 единиц
        HEIGHT: 1000,
    },
    SPAWN_AREA: {
        MIN_X: 100,
        MAX_X: 900,
        MIN_Y: 100,
        MAX_Y: 900,
    },
    BOUNDARIES: {
        MIN_X: 0,
        MAX_X: 1000,
        MIN_Y: 0,
        MAX_Y: 1000,
    },
};

// Параметры отображения (конвертация виртуальных координат в экранные)
// Коэффициенты рассчитываются динамически для каждого клиента
export const DISPLAY = {
    // Базовые размеры для расчетов соотношений
    BASE_SIZE: {
        WIDTH: 800,
        HEIGHT: 600,
    },
    // Соотношения сторон виртуального мира
    VIRTUAL_ASPECT_RATIO: 1000 / 1000,  // Квадратный мир
};

// Параметры игроков
export const PLAYER = {
    BASE_SCALE: 2, // Базовый масштаб спрайтов игроков
    ANIMATION_SPEED: 0.1, // Скорость анимации
};

// Другие настройки игры
export const GAME = {
    DEBUG_MODE: false, // Режим отладки
};